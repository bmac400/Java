The code uses standard java libraries so requires has no dependicies.
The code is in to files a main.java and a pagerank.java. The pagerank.java makes a page rank object that sets the lambda and tau, and then
can run on a map to return the list of page ranks. The pagerank object takes two parameters, and has one function pageRank(Map) which returns a
map of the page ranks.